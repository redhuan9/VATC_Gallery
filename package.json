{
  "name": "vatc-gallery",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --only hosting",
    "serve": "firebase serve"
  },
  "devDependencies": {
    "firebase-tools": "latest"
  }
}